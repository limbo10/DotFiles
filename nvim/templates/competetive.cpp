#include <bits/stdc++.h>

#define ll long long
#define INF INT_MAX
#define MOD 1e9+7
#define all(a) a.begin(), a.end()

using namespace std;

#define _DEBUG

int main() {
    std::ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    #ifdef _DEBUG
        freopen("ai", "r", stdin);
        freopen("ao", "w", stdout);
    #endif

    int tc {};
    cin >> tc;

    while (tc--) {
        
    }

    return 0;
}

